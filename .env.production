# Production environment variables
# These are embedded at build time by Vite

# API URL - uses /api prefix which nginx proxies to backend
# The actual backend URL is configured in nginx.conf via API_URL env var
VITE_API_URL=/api

# MSW mocks disabled in production
VITE_USE_MOCKS=false

# App environment
VITE_APP_ENV=production
